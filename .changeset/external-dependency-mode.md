---
"@koed/koed": minor
---

Added external dependency mode for `koed-server` so Koed can connect to Operator-managed Postgres, Redis/BullMQ, and Embedding Service resources without starting or stopping Docker Compose dependencies.
