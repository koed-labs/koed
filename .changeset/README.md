# Changesets

Koed currently uses one Changesets release unit: `@koed/koed`.

Add a changeset for release-noteworthy changes that should appear in the Koed
changelog. Select `@koed/koed` and choose the SemVer bump for the Docker Compose
deployment as a whole.

Do not select internal workspace packages unless they become separately
versioned release artifacts.
