# Shared Memory Surface Inventory

## Inventory Version

**Shared Memory surface inventory v1 (2026-07-20).** This version describes
the implemented external and internal surfaces. A record's `version`,
`grantVersion`, policy version, source revision, or realtime protocol version is
not an inventory version.

The authorization authority is the remote Team backend. A Personal API Token
has no Team-shared Memory authority. Unless a row below narrows the rule, every
read and realtime materialization must re-evaluate the authenticated browser
session or enrolled device's `team_workspace_read` operation family, enabled
Team Membership, active Team, current Workspace Access, active Share Grant,
source-owner consent, the source-owner/Team/Workspace representation-allowlist
intersection, and the current representation and content policies before a Team
decryption key is resolved. Team and Workspace path IDs, Share Grant ID,
logical Memory ID, and representation must all bind to that same authorization
decision. Missing, mismatched, revoked, archived, suspended, invalidated,
unknown-schema, placeholder, incomplete-provenance, or unauthorized state is
unavailable; no lower- or higher-fidelity representation is substituted.

## Surface Matrix

| Surface                                                | Shared-memory content                                                                                                                                                                   | Authorization and redaction boundary                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Representation behavior                                                                                                                                                                                                                                                                                                                                            | Test or implementation evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workspace list/index                                   | **Supported, metadata only.** No source body.                                                                                                                                           | Requires the universal read boundary and exact Team/Workspace path binding. Returns only active grants with an available or ordinarily stale representation. The DTO is bounded and omits owner-private principal/replica IDs, creator authority, encryption material, source content, and cross-Team correlation IDs.                                                                                                                                                                                      | Reports only the explicitly active representation, its state, source revision, freshness, timestamps, lifecycle, and grant-scoped companion scope. Invalidated, pending, unavailable, and revoked grants are absent.                                                                                                                                               | `apps/api/src/shared-memory/routes.ts` Workspace Share Grant list route; `apps/api/src/shared-memory/routes.test.ts` “returns a bounded metadata-only Workspace index”, “fails closed across Team and Workspace paths even when a repository mock returns data”, and “makes downgraded and revoked representations unavailable to future reads”; `packages/db/tests/shared-memory-repository.test.ts` “lists and reads active Memory Event, leaf, and rollup timelines from encrypted storage”.                                                                                                                                                   |
| Shared timeline/source page                            | **Supported.** Authorized redacted Memory Events, LCM leaves, or LCM rollups.                                                                                                           | Requires the universal read boundary for every page load. A cold read returns the newest bounded page first; older pages remain independently authorized and are followed asynchronously without polling. Returned source IDs are grant-scoped pseudonyms. Secret-bearing fields and prohibited source items are redacted or rejected before materialization; owner-private IDs and canonical cross-grant identifiers are not returned.                                                                     | A page contains exactly one active representation in strictly increasing source sequence. Memory Events retain permitted item kind, source time, and redacted structured content. Leaves and rollups contain summaries and provenance only for complete current coverage. LCM summaries do not acquire user/agent speakers. No representation fallback is allowed. | `packages/shared/src/collaboration-contract.ts` `sharedMemorySourcePageSchema`; `packages/db/tests/shared-memory-repository.test.ts` “redacts secrets and rejects unknown, prohibited, forged, and cross-source items”, “lists and reads active Memory Event, leaf, and rollup timelines from encrypted storage”, and “preserves exact rollup fidelity and policy provenance through owner-private sync and Team materialization”; `apps/api/src/local-edge/collaboration-command.test.ts` “dispatches Shared Memory source-page commands and returns the authorized page” and “rejects a fallback representation while selecting Shared Memory”. |
| Grant/session detail                                   | **Supported.** The active representation and authorized redacted items for one Share Grant.                                                                                             | Requires the universal read boundary and exact Team, Workspace, and Share Grant binding. The response is rebuilt from the authorized grant-scoped representation and omits owner-private replica/principal IDs, creator authority, encryption fields, credentials, and prohibited content.                                                                                                                                                                                                                  | Returns only the active representation. A requested representation that is not the active authorized one fails closed. Ordinary sync revocation may expose the last authorized revision as explicitly stale; safety or policy invalidation makes content unavailable.                                                                                              | `apps/api/src/shared-memory/routes.ts` scoped Share Grant GET route; `apps/api/src/shared-memory/routes.test.ts` “returns redacted read, index, and detail DTOs with companion scope” and “makes downgraded and revoked representations unavailable to future reads”; `packages/db/tests/shared-memory-repository.test.ts` “fails closed on every current access gate before resolving a decrypt key”.                                                                                                                                                                                                                                            |
| Item detail                                            | **Supported.** One redacted item already present in the authorized active representation.                                                                                               | Applies the same full grant read before matching the grant-scoped `sourceId`; guessed canonical, cross-grant, cross-Workspace, absent, or representation-substituted IDs return no item. The list endpoint exposes item metadata without content; the item-detail endpoint returns only the selected redacted item.                                                                                                                                                                                         | The item must match the active representation. Memory Event, leaf, and rollup identities are not interchangeable and cannot be used to obtain a higher-fidelity source.                                                                                                                                                                                            | `apps/api/src/shared-memory/routes.ts` scoped `/items` and `/items/:sourceId` routes; `apps/api/src/shared-memory/routes.test.ts` “returns redacted read, index, and detail DTOs with companion scope”; `packages/db/tests/shared-memory-repository.test.ts` “shares one canonical owner-private replica independently into two Teams without cross-grant correlation”.                                                                                                                                                                                                                                                                           |
| Graph                                                  | **Intentionally unavailable.** No Team-shared Memory graph DTO is implemented.                                                                                                          | Fail closed: after Team authentication, generic Personal-memory graph routes reject Team Workspace scope before repository access. They do not inherit Share Grant authority and cannot receive Team-shared source IDs or content.                                                                                                                                                                                                                                                                          | No nodes or edges are emitted. Grant-scoped `sourceIds` in an authorized summary are bounded provenance references, not graph access.                                                                                                                                                                                                                              | `apps/api/src/memory/graph-routes.ts` applies the Team Shared Memory surface guard to graph list and detail routes. `apps/api/src/server.test.ts` “keeps Team Shared Memory graph APIs unavailable behind Team authentication” proves Personal API Tokens fail authentication and authorized sessions/devices receive only the content-free unavailable response. Provenance confinement is independently covered by `packages/db/tests/shared-memory-repository.test.ts`.                                                                                                                                                                        |
| Graph expansion                                        | **Intentionally unavailable.**                                                                                                                                                          | Fail closed: after Team authentication, generic node expansion rejects Team Workspace scope before repository access. No grant-scoped pseudonym can expand into canonical, Personal, cross-session, cross-Workspace, revoked, or unrepresented nodes.                                                                                                                                                                                                                                                       | No expansion response and no representation traversal are provided.                                                                                                                                                                                                                                                                                                | `apps/api/src/memory/graph-routes.ts` applies the Team Shared Memory expansion guard. `apps/api/src/server.test.ts` “keeps Team Shared Memory expansion unavailable behind Team authentication” proves Personal API Tokens fail authentication, authorized sessions/devices receive only the unavailable response, and the repository is not called.                                                                                                                                                                                                                                                                                              |
| Evidence and citations                                 | **Intentionally unavailable as an Evidence Bundle or citation surface.** Authorized timeline/detail DTOs may include only the active representation's bounded, grant-scoped provenance. | Fail closed: after Team authentication, generic search and `memory_answer` reject Team Workspace scope before retrieval. A Share Grant alone cannot admit Team-shared content to Personal recall, Evidence Bundles, AI-client prompts, or citation resolvers. Owner-private and canonical source IDs remain hidden.                                                                                                                                                                                         | Summary `sourceIds`, counts, revisions, and provenance hashes establish representation integrity but cannot be dereferenced through a Team evidence API. No higher-fidelity source substitution is permitted.                                                                                                                                                      | `apps/api/src/memory/recall-routes.ts` applies the Team Shared Memory evidence guard. `apps/api/src/server.test.ts` “keeps Team Shared Memory evidence unavailable behind Team authentication” proves Personal API Tokens and wrongly scoped devices fail authentication, authorized sessions/devices receive only the unavailable response, and Team requests never reach retrieval. `docs/team-collaboration.md` keeps Team Chat and Shared Memory out of Personal recall.                                                                                                                                                                      |
| Conversation source manifest, segment, and live stream | **Supported only through a separate Conversation Source Access grant.** Exact verified source segments may be read; SSE events carry metadata only.                                     | Requires browser-session or scoped-device authentication plus the universal read boundary, active source-owner consent, active source grant, and exact artifact/segment binding. Personal API Tokens are rejected. Every read is reauthorized, and authorization loss closes live streams. The source grant is independent of semantic expansion level.                                                                                                                                                     | Snapshot mode pins a committed artifact frontier. Continuous mode follows verified generations of the same logical source. Opaque viewer-bound cursors resume durable database state after metadata-only PostgreSQL notifications; source bytes are fetched separately and are never placed in notification or SSE envelopes.                                      | `apps/api/src/team-conversation-source/routes.ts`; `packages/db/src/team-conversation-source-repository.ts`; API SSE socket tests and the real Postgres source-grant integration test cover replay, notification wake, generation rotation, snapshot pinning, revocation, consent expiry, and cross-principal denial.                                                                                                                                                                                                                                                                                                                             |
| Fork snapshot export                                   | **Supported as an explicit bounded Conversation source export.**                                                                                                                        | Requires a fresh browser session and the complete Conversation Source Access boundary. Generic Personal export authority, semantic Share Grant authority, and API Tokens do not imply fork authority.                                                                                                                                                                                                                                                                                                       | Returns verified NDJSON only through a completed turn boundary, with parent lineage and digest headers. It does not mutate the owner's Conversation, rewrite provider records, or create a Koed-side child automatically.                                                                                                                                          | `POST /v1/shared-memory/share-grants/:shareGrantId/transcript/fork-snapshot` in `apps/api/src/team-conversation-source/routes.ts`; route tests cover authentication, bounds, lifecycle denial, and incomplete-turn rejection.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Realtime snapshot and events                           | **Supported for authorized DTO updates; source plaintext is not carried in the durable event envelope.**                                                                                | Snapshot/stream authentication rejects Personal API Tokens and binds opaque cursors to principal, backend identity, scope, Team, client instance, subscription, and protocol version. Each event is authorized after materialization and again before serialization. Authorization loss stops replay and causes removal or an authoritative snapshot requirement.                                                                                                                                           | Share Grant events are withheld until an authorized representation exists. Availability produces a grant-scoped Shared Session upsert; revocation removes that session without exposing content or terminating unrelated Team scope. Clients refresh source pages through authorized commands.                                                                     | `apps/api/src/collaboration/realtime.test.ts` “skips an initial Share Grant event until an authorized representation exists”, “materializes an authorized Shared Session when its representation becomes available”, “removes only the revoked Shared Session and leaves the Team stream active”, “reauthorizes each event after materialization and before serialization”, and “stops a multi-event, multi-batch replay before serializing any DTO after authorization loss”; `apps/api/src/local-edge/collaboration-realtime-broker.test.ts` rejects mismatched Shared Session/Share Grant identities.                                          |
| Diagnostics/status                                     | **Intentionally unavailable for Shared Memory content.** Content-free operational state is permitted.                                                                                   | Diagnostics may expose bounded counts, health/state classes, policy or revision identifiers, and safe error classes only to their separately authorized operational audience. They must omit source text, summaries, canonical/owner-private identifiers, credentials, encryption material, raw provider errors, and cross-Team correlation. Diagnostic authority never grants Shared Memory read authority.                                                                                                | Diagnostics report state, not a representation. They cannot preview, reconstruct, substitute, or dereference any representation.                                                                                                                                                                                                                                   | `apps/api/src/shared-memory/routes.test.ts` safe DTO and error-mapping cases; repository policy audit tests in `packages/db/tests/shared-memory-repository.test.ts` verify content-free audit metadata. No Shared Memory content diagnostic route exists in `apps/api/src/shared-memory/routes.ts`.                                                                                                                                                                                                                                                                                                                                               |
| API, broker, IPC, and renderer errors                  | **Intentionally unavailable for Shared Memory content.**                                                                                                                                | Normal callers receive fixed status classes/messages such as unauthorized, conflict, rejected source, or not found. Raw upstream bodies, stack traces, internal IDs, credentials, URLs containing secrets, plaintext source details, and configuration instructions do not cross the boundary.                                                                                                                                                                                                              | Errors never include a fallback representation or stale protected payload. An unavailable representation remains an unavailable shell until an authorized current representation can be loaded.                                                                                                                                                                    | `apps/api/src/shared-memory/routes.ts` error mapping; `apps/api/src/shared-memory/routes.test.ts` “maps owner authorization and optimistic conflicts without repository detail”; `apps/api/src/local-edge/collaboration-command.test.ts` fail-closed Shared Memory authority cases; `apps/desktop/src/collaboration/renderer-client.test.ts` “preserves the Shared Memory shell and companion while a representation is unavailable”.                                                                                                                                                                                                             |
| Logs, audit, and metrics                               | **Intentionally unavailable for Shared Memory plaintext.** Content-free security and operational records are supported.                                                                 | Records may contain scoped structural IDs or pseudonyms, hashes, counts, timing, action/error class, actor reference, and policy/revision metadata. They must not contain source text, summaries, decrypted metadata, credentials, invite/action tokens, DEKs, raw upstream payloads, or canonical cross-Team identifiers. Access to logs or audit does not grant representation access.                                                                                                                    | Records describe actions and revisions, never serialize a representation or provide representation traversal.                                                                                                                                                                                                                                                      | `packages/db/tests/shared-memory-repository.test.ts` “audits representation policies and publishes committed Shared Memory outbox notifications” and “audits policy reduction, invalidates the active representation, and never falls back”; API logging safeguards are exercised in `apps/api/src/server/logging.test.ts`.                                                                                                                                                                                                                                                                                                                       |
| Queues, outbox, replay, and retry custody              | **Intentionally unavailable for plaintext Shared Memory.** Structural notifications and encrypted references are supported.                                                             | Durable outbox/retry data is limited to structural scope/resource IDs, event family, encrypted payload references where applicable, and bounded replay metadata. Plaintext source/chat bodies, summaries, credentials, keys, and decrypted metadata are prohibited. Work is re-authorized when materialized or replayed; loss of authority stops serialization.                                                                                                                                             | Queue/outbox entries signal invalidation or rematerialization and do not constitute a representation. They cannot be replayed as higher-fidelity content or bypass the active representation.                                                                                                                                                                      | `packages/db/src/shared-memory-repository.ts` collaboration outbox writes; `packages/db/tests/shared-memory-repository.test.ts` policy/outbox audit tests and encrypted-storage timeline test; `apps/api/src/collaboration/realtime.test.ts` content-safe replay, authorization-loss, listener-restart, and durable-outbox replay tests.                                                                                                                                                                                                                                                                                                          |
| Broker, preload, IPC, and renderer caches              | **No persistent protected Shared Memory content is supported.** Bounded in-memory authorized view state is supported.                                                                   | The renderer receives schema-validated authorized DTOs, never reusable remote credentials or owner-private replica material. Protected in-memory state, stale responses, queued writes, selections, and IPC result custody are cleared or rejected on grant/policy/representation change, Workspace or membership loss, session/device revocation, disconnect, or remote identity change. `localStorage`, `sessionStorage`, service-worker storage, and persistent browser caches are not authority stores. | In-memory state contains only the selected active representation. A representation change purges old items before replacement; an unavailable replacement retains only a non-content shell and independently authorized companion discussion.                                                                                                                      | `apps/desktop/src/collaboration/renderer-client.test.ts` “purges an open higher-fidelity source before applying a representation downgrade”, “preserves the Shared Memory shell and companion while a representation is unavailable”, and stale/revocation selection tests; `apps/desktop/src/collaboration/renderer-surface.test.tsx` renderer boundary coverage; `apps/api/src/local-edge/collaboration-realtime-broker.test.ts` Share Grant revocation and envelope-binding tests.                                                                                                                                                             |

## Representation Contract

- `memory_events` exposes only independently permitted canonical source items
  after classification and redaction, with exact permitted item kind, order,
  source time, and bounded structured metadata.
- `lcm_leaves` exposes only complete, current LCM Summary leaves whose provenance
  exactly covers the authorized source revision.
- `lcm_rollups` exposes only complete, current LCM Summary rollups whose child
  and source provenance exactly covers the authorized source revision.
- Missing, partial, LCM Placeholder, unknown-schema, forged-eligibility,
  cross-session, policy-invalidated, and safety-stale material is unavailable.
  Ordinary sync interruption may preserve the last authorized revision with an
  explicit stale state. Safety or authority invalidation removes protected
  content until a compliant representation is materialized.
