# TODO

- Package Desktop with a self-contained Koed runtime so packaged startup no longer depends on a source checkout (`KOED_REPO_ROOT`) or workspace `pnpm` scripts. Current PR guards this path and requires an explicit checkout/runtime until packaged API/Worker/Explorer artifacts are shipped.
