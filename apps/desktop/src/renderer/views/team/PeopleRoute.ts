/**
 * Shell-neutral Team People workflow.
 *
 * Kept as a stable import boundary while the compatibility renderer is
 * removed. Authority-sensitive controls remain driven by the optional
 * management payload and current snapshot.
 */
export { PeopleView as TeamPeopleRoute } from "../../collaboration/CollaborationRoutesImpl.js";
